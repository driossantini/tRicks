% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ms_utils.R
\name{save_image}
\alias{save_image}
\title{Save plot image using ggplot2 or base graphics}
\usage{
save_image(
  filename,
  object,
  scale = 1,
  width = NULL,
  height = NULL,
  units = c("in", "cm", "mm", "pt"),
  dpi = 300,
  engine = c("gg", "rgraphics"),
  verbose = FALSE,
  ...
)
}
\arguments{
\item{filename}{Character string specifying the file path for the PDF.}

\item{object}{The object to save.}

\item{scale}{Numeric scaling factor (used by \code{ggsave()}).}

\item{width}{Numeric width of the output.}

\item{height}{Numeric height of the output.}

\item{units}{Character string specifying the units for width and height.
Must be one of \code{"in"}, \code{"cm"}, \code{"mm"}, or \code{"pt"}.}

\item{dpi}{Numeric DPI used by \code{ggsave()}. Ignored when using base graphics.}

\item{engine}{Character string specifying the engine to use for saving.
Either \code{"gg"} (ggplot2) or \code{"rgraphics"} (base R). If \code{"gg"} is selected
but the object is not compatible, base graphics will be used as a fallback.}

\item{verbose}{Logical; if \code{TRUE}, enables debugging messages such as the selected engine
and additional processing information.}

\item{...}{Additional arguments passed to the underlying plotting function.}
}
\value{
No return value; the function is called for its side effects.
}
\description{
Save a plot object as a PDF using ggplot2 or base R

This function attempts to use \code{ggsave()} for objects whose class starts with \code{"gg"}.
If \code{ggsave()} fails, or if the object is not ggplot-based, it falls back to base R
graphics using \code{pdf()} and \code{plot()}. Note that in the fallback branch, \code{pdf()} is
only called once and plots a single object.
}
