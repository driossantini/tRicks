% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multisave.R
\name{multisave}
\alias{multisave}
\title{Save multiple objects}
\usage{
multisave(
  object,
  output_dir,
  name,
  formats = NULL,
  scale = 1,
  width = NULL,
  height = NULL,
  units = c("in", "cm", "mm", "pt"),
  dpi = 300,
  engine = c("gg", "rgraphics"),
  verbose = FALSE,
  ...
)
}
\arguments{
\item{object}{The object to be saved.}

\item{output_dir}{Character string specifying the root output directory.
Subdirectories for each format will be created here.}

\item{name}{Character string specifying the base file name without extension.}

\item{formats}{Character vector specifying which formats to save the object as.
For example: \code{c("png", "pdf", "RDS")}. Format names are case-insensitive.}

\item{scale}{Numeric scaling factor applied to the width and height. A value of 1
indicates no scaling.}

\item{width}{Numeric value specifying the width of the plot. Its value is interpreted
in the units specified by the \code{units} parameter.}

\item{height}{Numeric value specifying the height of the plot. Its value is interpreted
in the units specified by the \code{units} parameter.}

\item{units}{Character string representing the units for the width and height. Accepted
values are \code{"in"}, \code{"cm"}, \code{"mm"}, or \code{"pt"}.}

\item{dpi}{Numeric value representing the resolution in dots per inch. This is used by
raster-based output and is ignored for vector outputs such as PDF.}

\item{engine}{Character string indicating the plotting engine to use. For instance, if set to \code{"gg"},
the function will attempt to use \code{ggsave()} for ggplot-based objects, and if that fails
it will fall back to base R graphics.}

\item{verbose}{Logical; if \code{TRUE}, enables debugging messages such as the selected engine
and additional processing information.}

\item{...}{Additional arguments passed to the underlying save functions.}
}
\value{
A named list of file paths for each saved format that can be piped to \code{version_control()}.
}
\description{
Save an object in multiple formats
This function saves a given graphics object into multiple formats.
It creates subdirectories for each format such as PNG, PDF, or RDS
under the specified output directory and saves the object
with the given base name.
}
\examples{
\dontrun{
  # Save 'myplot' in multiple formats in 'mydir' with 'myplot' as the base file name.
  my_images <- multisave(object = myplot,
                         formats = c("png", "pdf", "RDS"),
                         output_dir = "mydir",
                         name = "myplot",
                         scale = 1,
                         width = 6,
                         height = 4,
                         units = "in",
                         dpi = 300,
                         engine = "gg",
                         verbose = FALSE)

  # Optionally, pass the resulting file paths to version_control() for versioning:
  my_images |> version_control(TRUE, "versions")
}
}
